# 分歧度测试

决定 critic-individualist 和 critic-contrastivist 是否值得留下。

这个测试只能否决，不能认证。通过它只说明两个视角的差异比噪声大，不说明这些意见正确、有用、或者值回 token。那一步只有你能判断。

**这不是日常用法。** 两个 critic 一旦完成校准，平时按稿件风险选择性调用即可，不要为了“完整”而每篇重复四次。

分两级。第一级很便宜，不通过就直接删掉一个 agent，不必做第二级。

## 材料

一份你已经定稿、并且清楚它薄弱处在哪的旧稿。「结构的替身」合适：它同时涉及结构性解释和经验现象，两个立场都咬得住，而且你对它的判断足够稳定，能当对照标准。

不要用正在写的稿子。你对它的判断还在变，会把 agent 的表现和你自己的犹豫混在一起。

## 拆解规则

每份报告拆成原子指控。一条原子指控 = 一处被攻击的稿件位置上的一个可独立成立或不成立的理由。

不要按报告里的编号计。实际运行中，agent 会把三四处段落打包成一个编号项，而打包习惯每次运行都不同——按编号计，你测到的是打包习惯的差异，不是立场的差异。一个编号项里攻击了三处段落，就拆成三条。

跨轮匹配以引文文本为准，不以段号为准。agent 给出的段号无法确认是数出来的还是估的，两次运行给同一处配不同段号时，重合会被误判成独有，B 被虚假抬高。

两条指控的配对关系只用三类：

- **重合**：指向稿件同一处，且理由实质相同。措辞不同不算差异。
- **同处异因**：指向同一处，但依据的前提不同，得出的修改方向也不同。
- **独有**：只有一边提出。

配对必须是一对一：一条原子指控在一次两两比较中最多参与一个配对。先按稿件引文定位候选，再判断理由属于重合还是同处异因；无法配对的两边条目都计入独有数。不要让一条宽泛指控同时“覆盖”另一份报告里的三条具体指控，否则分母和分子都会失真。

两份报告之间的差异度记作 d，定义为：

```
d = (同处异因数 + 独有数) / (重合数 + 同处异因数 + 独有数)
```

如果两份报告都没有原子指控，定义 `d = 0`；如果只有一份为空，所有非空条目都算独有，因此 `d = 1`。每次计算同时记录分子、分母和最终小数，不要只抄一个四舍五入后的 d。

遇到确实无法确定是“重合”还是“同处异因”的配对，不要强行选。分别按两种分类计算上下界；只有结论在上下界两端都不改变时才进入下一步，否则本轮结果记为不确定并复核分类。

## 每次运行的有效性检查

跑完一次就先查这个，不合格的运行不要计入 W 或 B，重跑。runner 会自动拒绝能确定判断的结构性违规：

```bash
python critic_runner.py validate critic-individualist path/to/report.md
```

`run` 模式也会自动执行同一校验；执行器返回 0 但报告结构无效时，runner 返回 3。下面前五项中的标题、编号、一一对应和失败出口由 runner 检查，引文是否真来自稿件、理由是否原子化等语义条件仍由人复核。

- 六节输出骨架都在且顺序正确，A 编号连续，没有把多处稿件位置合并成一条。
- 第二节与第一节的 A 编号完全相同，每条原子指控都有一次且只有一次后果检验。
- 最弱一步只有一个，没有并列。
- 最强论证一节存在，无论结论是指认了某处还是明说“无一处满足”。
- `STATUS` 和 `UNVERIFIED` 都在。

这一步不能省。实测中出现过第二节只覆盖了一部分条目的情况，那次运行少干了活，而少干的活会伪装成判断的随机性，把 W 抬高。W 被抬高的后果是你按 B ≤ W 删掉一个其实合格的 agent——把一个改提示词就能修的合规漏洞，误判成立场不成立。方向正好反了。

## 第一级：W/B 廉价否决

这一级的存在理由是：如果同一个 agent 重跑两次的差异，和两个不同 agent 之间的差异一样大，那么所谓"视角差异"只是采样噪声，后面所有测试都没有意义。

四次运行，每次独立开始，不让任何一次看到其他次的输出：

```
I₁, I₂  = critic-individualist 跑两次
C₁, C₂  = critic-contrastivist 跑两次
```

用独立 runner 时，可以逐条执行 `run`，也可以让 `campaign` 串行完成四次隔离运行：

```bash
python critic_runner.py campaign draft.md --repeat 2 -- <executor...>
```

四次报告全部有效时，它会生成 I₁、I₂、C₁、C₂ 的独立归档，以及已经列出各报告全部 A 条目的 `scorecard.json`。这只是编排，不是并发，也不会把前一份报告放进后一份提示词。等价的逐条命令是：

```bash
python critic_runner.py run critic-individualist draft.md -- <executor...>
python critic_runner.py run critic-individualist draft.md -- <executor...>
python critic_runner.py run critic-contrastivist draft.md -- <executor...>
python critic_runner.py run critic-contrastivist draft.md -- <executor...>
```

不要把四次运行包进并发任务。这个测试测的是输出差异，不需要并发；串行不会损失测试信息。

盲分时只在六组比较的 `pairs` 数组里记录左右 A 编号，并把分类写成 `overlap`、`different_reason` 或 `ambiguous`；不要手算汇总，无法确定时也不要强选。每组检查完成后显式设置 `complete: true`。未参与配对的左右条目由工具自动计为独有。机器会从逐条配对推导计数并计算上下界：

```bash
python critic_runner.py score path/to/scorecard.json --format markdown
```

`score` 会先用归档报告的 hash 和重新提取结果复核 scorecard 的 claim 清单，并强制一对一配对。只有整个区间都跨过同一判据时才给 `reject` 或 `advance`；边界两端会改变结论时给 `inconclusive`。语义分类仍由人完成，工具只复算分子、分母、W/B 与阈值。

组内差异（同一 agent 自己跟自己的分歧）：

```
W = [ d(I₁,I₂) + d(C₁,C₂) ] / 2
```

组间差异（两个 agent 之间的分歧）：

```
B = [ d(I₁,C₁) + d(I₁,C₂) + d(I₂,C₁) + d(I₂,C₂) ] / 4
```

判据：

- **B ≤ W**：否决。两个立场之间的差异不比同一个模型随机重跑的差异更稳定。删掉一个，保留另一个作为单一 critic。到此为止。
- **B 明显大于 W**（经验上取 B ≥ W + 0.2）：进入第二级。

分类时把 agent 名字从报告上遮掉再做。你知道哪份是哪个 agent 写的，就会倾向于把措辞差异读成立场差异。

## 第二级：框架承诺是否真的起作用

第一级只证明了两个 agent 不一样，没证明这个不一样来自框架承诺——也可能只是两份长提示词各自的随机偏向。

用 `test/critic-generic.md` 作为对照。它的输出结构、工作量要求和禁止事项与两个专用 agent 完全对齐，唯一的区别是没有框架承诺。这一点必须保持——如果对照件只写一句"找出问题"，专用 agent 必然产出更多更细的指控，你算出来的覆盖率里混着提示词详细程度的差异，而不只是框架差异。同一份稿件跑两次，得到 G₁、G₂。

runner 默认拒绝执行这个测试件，防止误拿它做日常审稿。第二级要显式解锁：

```bash
python critic_runner.py run critic-generic draft.md --allow-test-artifact -- <executor...>
```

计算覆盖率之前先去重。把 I₁、I₂、C₁、C₂ 的所有指控按"重合"关系合并成一个集合，同一条稳定指控无论出现几次都只算一条。不去重的话，专用 agent 里最稳定的那些指控会在分母里被重复计入四次，虚假压低 generic 的覆盖率。G₁、G₂ 同样合并。

```
覆盖率 = 被 generic 集合覆盖的专用指控数 / 去重后的专用指控总数
```

- 覆盖率高于 0.7：框架承诺没起作用。你在系统提示上花的功夫是白费的，直接用通用 critic，成本低得多。
- 覆盖率低于 0.4：两个立场确实产出了通用 critic 到不了的地方，保留。
- 中间区段：看那些未被覆盖的指控具体是什么。如果它们集中在一个 agent 身上，只留那一个。

## 四项不计入公式但要记录的

**最弱一步与最强论证的稳定性。** 两个 agent 都被要求排序指认唯一的最弱一步，以及唯一的最强论证。分别记录四次运行的结果。

实测数据：最弱一步四次运行给出四个不同答案（individualist ¶36 与 ¶64，contrastivist ¶38 与 ¶34）。这一项是噪声，不要读，也不要指望“两个 agent 指向同一步”能说明什么——单个 agent 自己都复现不了的判断，跨 agent 的偶然一致不构成证据。

最强论证在 individualist 上两次一致（都指 ¶32／¶40），在 contrastivist 上两次不同（¶42／¶48 与 ¶62–64）。所以稳定性是 agent 特有的，不是这一项设计的普遍性质。判断某一项能不能读，唯一的办法是先看该 agent 自己的组内是否复现，不能从别的 agent 的表现外推。

**同一段落被两个 agent 给出相反判决。** 这是比数字更有说服力的信号。实测中 ¶62–64 被 individualist 判为全篇最弱、被 contrastivist 判为全篇最强，两边的理由在各自前提下都成立。出现这种倒转，说明框架承诺是真实的；戏服式的 critic 不会在承重段落上撞成这样。记录所有此类倒转。

**critic 自己引入的文献。** 两个 critic 在论证时可能引入外部文献作为支撑（实证效应、研究结论、他人立场）。这些引用不经过 citation-auditor，全部未审。记录它们，但不要因为报告里出现了一个具名研究就提高对该条指控的信任度。

**有没有承认局部存活。** 两个 agent 都被强制要求指认"全篇在本立场下最强的一处论证"。检查这一项：如果四次运行里它们都写"无一处满足"，而你知道这份旧稿确实有符合该框架要求的段落，那说明它们在制造异议以显得有产出。

不要用"整篇无实质异议"作为这个信号。那是全局结论，稿件只要存在一个真实弱点，四次都不出现空结论完全正常，推不出任何东西。要测的是它能不能承认局部论证存活，不是它会不会放弃攻击整篇。

这个信号比分歧度低更严重。分歧度低只是浪费钱；拒绝承认存活意味着报告里每一条你都不能直接采信。

## 有效性

上面全部通过之后，剩下的问题测不出来：这些意见是不是对的、是不是有用。两个方向不同的胡说八道也能拿到很高的 B 值。

唯一的判据是事后的：跑完测试的三个月里，记录有多少条指控真的导致你修改了论证。低于三分之一，就把这两个 agent 降级成偶尔调用，不要放进常规流程。
