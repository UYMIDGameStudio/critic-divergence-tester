# 分歧度测试

决定 critic-individualist 和 critic-contrastivist 是否值得留下。

这个测试只能否决，不能认证。通过它只说明两个视角的差异比噪声大，不说明这些意见正确、有用、或者值回 token。那一步只有你能判断。

分两级。第一级很便宜，不通过就直接删掉一个 agent，不必做第二级。

## 材料

一份你已经定稿、并且清楚它薄弱处在哪的旧稿。「结构的替身」合适：它同时涉及结构性解释和经验现象，两个立场都咬得住，而且你对它的判断足够稳定，能当对照标准。

不要用正在写的稿子。你对它的判断还在变，会把 agent 的表现和你自己的犹豫混在一起。

## 拆解规则

每份报告拆成原子指控。一条指控 = 一个可以单独成立或不成立的主张，通常对应报告里的一个编号项。

两条指控的配对关系只用三类：

- **重合**：指向稿件同一处，且理由实质相同。措辞不同不算差异。
- **同处异因**：指向同一处，但依据的前提不同，得出的修改方向也不同。
- **独有**：只有一边提出。

两份报告之间的差异度记作 d，定义为：

```
d = (同处异因数 + 独有数) / (重合数 + 同处异因数 + 独有数)
```

## 第一级：W/B 廉价否决

这一级的存在理由是：如果同一个 agent 重跑两次的差异，和两个不同 agent 之间的差异一样大，那么所谓"视角差异"只是采样噪声，后面所有测试都没有意义。

四次运行，每次独立开始，不让任何一次看到其他次的输出：

```
I₁, I₂  = critic-individualist 跑两次
C₁, C₂  = critic-contrastivist 跑两次
```

组内差异（同一 agent 自己跟自己的分歧）：

```
W = [ d(I₁,I₂) + d(C₁,C₂) ] / 2
```

组间差异（两个 agent 之间的分歧）：

```
B = [ d(I₁,C₁) + d(I₁,C₂) + d(I₂,C₁) + d(I₂,C₂) ] / 4
```

判据：

- **B ≤ W**：否决。两个立场之间的差异不比同一个模型随机重跑的差异更稳定。删掉一个，保留另一个作为单一 critic。到此为止。
- **B 明显大于 W**（经验上取 B ≥ W + 0.2）：进入第二级。

分类时把 agent 名字从报告上遮掉再做。你知道哪份是哪个 agent 写的，就会倾向于把措辞差异读成立场差异。

## 第二级：框架承诺是否真的起作用

第一级只证明了两个 agent 不一样，没证明这个不一样来自框架承诺——也可能只是两份长提示词各自的随机偏向。

用 `test/critic-generic.md` 作为对照。它的输出结构、工作量要求和禁止事项与两个专用 agent 完全对齐，唯一的区别是没有框架承诺。这一点必须保持——如果对照件只写一句"找出问题"，专用 agent 必然产出更多更细的指控，你算出来的覆盖率里混着提示词详细程度的差异，而不只是框架差异。同一份稿件跑两次，得到 G₁、G₂。

计算覆盖率之前先去重。把 I₁、I₂、C₁、C₂ 的所有指控按"重合"关系合并成一个集合，同一条稳定指控无论出现几次都只算一条。不去重的话，专用 agent 里最稳定的那些指控会在分母里被重复计入四次，虚假压低 generic 的覆盖率。G₁、G₂ 同样合并。

```
覆盖率 = 被 generic 集合覆盖的专用指控数 / 去重后的专用指控总数
```

- 覆盖率高于 0.7：框架承诺没起作用。你在系统提示上花的功夫是白费的，直接用通用 critic，成本低得多。
- 覆盖率低于 0.4：两个立场确实产出了通用 critic 到不了的地方，保留。
- 中间区段：看那些未被覆盖的指控具体是什么。如果它们集中在一个 agent 身上，只留那一个。

## 两项不计入公式但要记录的

**最弱一步是否同一步。** 两个 agent 都被要求排序指认全篇最弱的一步。如果它们指向同一步，那一步大概率真的是最弱处，值得你优先处理，无论上面的数字如何。

**有没有承认局部存活。** 两个 agent 都被强制要求指认"全篇在本立场下最强的一处论证"。检查这一项：如果四次运行里它们都写"无一处满足"，而你知道这份旧稿确实有符合该框架要求的段落，那说明它们在制造异议以显得有产出。

不要用"整篇无实质异议"作为这个信号。那是全局结论，稿件只要存在一个真实弱点，四次都不出现空结论完全正常，推不出任何东西。要测的是它能不能承认局部论证存活，不是它会不会放弃攻击整篇。

这个信号比分歧度低更严重。分歧度低只是浪费钱；拒绝承认存活意味着报告里每一条你都不能直接采信。

## 有效性

上面全部通过之后，剩下的问题测不出来：这些意见是不是对的、是不是有用。两个方向不同的胡说八道也能拿到很高的 B 值。

唯一的判据是事后的：跑完测试的三个月里，记录有多少条指控真的导致你修改了论证。低于三分之一，就把这两个 agent 降级成偶尔调用，不要放进常规流程。
